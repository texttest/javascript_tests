# Fallback site-specific config file, if there isn't one at site/siteconfig/site_configfile
# We point the checkout as if a tarball has just been downloaded

checkout_location:${TEXTTEST_HOME}/../source

# Need to set this to somewhere in the file system where the file can be picked up via http in a browser
# default_texttest_local_tmp:/some_http_path
